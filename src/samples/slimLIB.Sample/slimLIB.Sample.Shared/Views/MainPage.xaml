<Page
    x:Class="slimLIB.Sample.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:slimLIB.Sample.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Top"
					Margin="20">
            <TextBlock Text="Please enter your name"
					   Foreground="Red" />
            <TextBox Text="{Binding [Name], Mode=TwoWay}" />
            <TextBlock>
                <Run Text="Hello" />
                <Run Text="{Binding [Name]}" />
            </TextBlock>
            <TextBlock Text="The current time and date is" />
            <TextBlock Text="{Binding [DateAndTime]}" />
            <Button Content="Clear"
					Command="{Binding [ClearCommand]}" />
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom">
            <TextBlock Text="Number fact:" />
            <TextBlock Text="{Binding [Number]}" />
        </StackPanel>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Bullets"
						  Label="numbers"
						  Command="{Binding [TestNumbers]}" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
